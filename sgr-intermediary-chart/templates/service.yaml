apiVersion: v1
kind: Service
metadata:
  name: {{ include "sgr-intermediary-chart.fullname" . }}
  labels:
    {{- include "sgr-intermediary-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sgr-intermediary-chart.selectorLabels" . | nindent 4 }}
